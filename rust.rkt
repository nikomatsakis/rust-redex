
;; The file has been split into separate modules
;; plus test modules. The 'types.rkt' file needs
;; to be reorganized accordingly and we should
;; check why it fails (in possibly unpredictable
;; ways)
